<!-- res/color/text_color_selector.xml -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_selected="true" android:color="@android:color/white" /> <!-- Selected (pressed) state -->
    <item android:color="@android:color/black" /> <!-- Default state (not selected) -->
</selector>
